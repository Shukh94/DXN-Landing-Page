---
deployment:
  tasks:
    # Step 1: Deploy path define করো
    - export DEPLOYPATH=/home/shakilas/repositories/DXN-Landing-Page

    # Step 2: Git থেকে সব ফাইল ওই ফোল্ডারে কপি করো
    - /bin/rsync -av * $DEPLOYPATH --exclude '.git' --exclude '.cpanel.yml'
